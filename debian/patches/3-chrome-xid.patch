Index: iridium-browser/chrome/browser/ui/views/frame/browser_frame.cc
===================================================================
--- iridium-browser.orig/chrome/browser/ui/views/frame/browser_frame.cc	2015-02-02 15:02:32.000000000 +0100
+++ iridium-browser/chrome/browser/ui/views/frame/browser_frame.cc	2015-02-02 15:03:03.000000000 +0100
@@ -35,6 +35,7 @@
 #include "ui/gfx/screen.h"
 #include "ui/views/controls/menu/menu_runner.h"
 #include "ui/views/widget/native_widget.h"
+#include "ui/views/widget/native_widget_aura.h"
 
 #if defined(OS_LINUX) && !defined(OS_CHROMEOS)
 #include "chrome/browser/shell_integration_linux.h"
@@ -183,6 +184,12 @@
   return browser_frame_view_;
 }
 
+void BrowserFrame::SendSessionIdToWindow() {
+  const Browser& browser = *browser_view_->browser();
+  views::NativeWidget* a = native_browser_frame_->AsNativeWidget();
+  a->SetSessionID(browser.session_id().id());
+}
+
 bool BrowserFrame::UseCustomFrame() const {
   return use_custom_frame_pref_.GetValue() &&
       browser_view_->IsBrowserTypeNormal();
Index: iridium-browser/chrome/browser/ui/views/frame/browser_frame.h
===================================================================
--- iridium-browser.orig/chrome/browser/ui/views/frame/browser_frame.h	2015-02-02 15:02:32.000000000 +0100
+++ iridium-browser/chrome/browser/ui/views/frame/browser_frame.h	2015-02-02 15:03:03.000000000 +0100
@@ -86,6 +86,8 @@
   void GetWindowPlacement(gfx::Rect* bounds,
                           ui::WindowShowState* show_state) const;
 
+  void SendSessionIdToWindow();
+
   // Overridden from views::Widget:
   views::internal::RootView* CreateRootView() override;
   views::NonClientFrameView* CreateNonClientFrameView() override;
Index: iridium-browser/chrome/browser/ui/views/frame/browser_view.cc
===================================================================
--- iridium-browser.orig/chrome/browser/ui/views/frame/browser_view.cc	2015-02-02 15:02:32.000000000 +0100
+++ iridium-browser/chrome/browser/ui/views/frame/browser_view.cc	2015-02-02 15:03:03.000000000 +0100
@@ -630,6 +630,7 @@
   force_location_bar_focus_ = false;
 
   browser()->OnWindowDidShow();
+  frame_->SendSessionIdToWindow();
 
   chrome::MaybeShowInvertBubbleView(this);
 }
Index: iridium-browser/ui/aura/client/aura_constants.cc
===================================================================
--- iridium-browser.orig/ui/aura/client/aura_constants.cc	2015-02-02 15:02:32.000000000 +0100
+++ iridium-browser/ui/aura/client/aura_constants.cc	2015-02-02 15:03:03.000000000 +0100
@@ -8,6 +8,7 @@
 #include "ui/gfx/rect.h"
 
 DECLARE_EXPORTED_WINDOW_PROPERTY_TYPE(AURA_EXPORT, bool)
+DECLARE_EXPORTED_WINDOW_PROPERTY_TYPE(AURA_EXPORT, uint32)
 DECLARE_EXPORTED_WINDOW_PROPERTY_TYPE(AURA_EXPORT, ui::ModalType)
 DECLARE_EXPORTED_WINDOW_PROPERTY_TYPE(AURA_EXPORT, gfx::Rect*)
 DECLARE_EXPORTED_WINDOW_PROPERTY_TYPE(AURA_EXPORT, ui::InputMethod*)
@@ -35,6 +36,7 @@
 DEFINE_WINDOW_PROPERTY_KEY(ui::InputMethod*, kRootWindowInputMethodKey, NULL);
 DEFINE_WINDOW_PROPERTY_KEY(
     ui::WindowShowState, kShowStateKey, ui::SHOW_STATE_DEFAULT);
+DEFINE_WINDOW_PROPERTY_KEY(uint32, kSessionIdKey, 0);
 
 }  // namespace client
 }  // namespace aura
Index: iridium-browser/ui/aura/client/aura_constants.h
===================================================================
--- iridium-browser.orig/ui/aura/client/aura_constants.h	2015-02-02 15:02:32.000000000 +0100
+++ iridium-browser/ui/aura/client/aura_constants.h	2015-02-02 15:03:03.000000000 +0100
@@ -65,6 +65,9 @@
 AURA_EXPORT extern const WindowProperty<ui::WindowShowState>* const
     kShowStateKey;
 
+AURA_EXPORT extern const WindowProperty<uint32>* const kSessionIdKey;
+
+
 // Alphabetical sort.
 
 }  // namespace client
Index: iridium-browser/ui/aura/window_tree_host_x11.cc
===================================================================
--- iridium-browser.orig/ui/aura/window_tree_host_x11.cc	2015-02-02 15:02:32.000000000 +0100
+++ iridium-browser/ui/aura/window_tree_host_x11.cc	2015-02-02 15:03:03.000000000 +0100
@@ -485,6 +485,26 @@
   return xwindow_;
 }
 
+#if 0
+void WindowTreeHostX11::SetSessionID(uint32 session_id) {
+  VLOG(1) << "BROKEN delete this session writer.";
+
+  if (session_id != 0) {
+    XChangeProperty(xdisplay_,
+                    xwindow_,
+                    atom_cache_.GetAtom("CHROMIUM_LINUX_SESSION_ID_PROPERTY"),
+                    XA_CARDINAL,
+                    32,
+                    PropModeReplace,
+                    reinterpret_cast<unsigned char*>(session_id),
+                    1);
+    VLOG(1) << " DID  set session_id as window property because we have a session " << session_id;
+  } else {
+    VLOG(1) << "can't set session_id as window property because we don't have a session yet.";
+  }
+}
+#endif
+
 void WindowTreeHostX11::Show() {
   if (!window_mapped_) {
     // Before we map the window, set size hints. Otherwise, some window managers
Index: iridium-browser/ui/aura/window_tree_host_x11.h
===================================================================
--- iridium-browser.orig/ui/aura/window_tree_host_x11.h	2015-02-02 15:02:32.000000000 +0100
+++ iridium-browser/ui/aura/window_tree_host_x11.h	2015-02-02 15:03:03.000000000 +0100
@@ -41,6 +41,8 @@
   bool CanDispatchEvent(const ui::PlatformEvent& event) override;
   uint32_t DispatchEvent(const ui::PlatformEvent& event) override;
 
+//  void SetSessionID(uint32 s) OVERRIDE;
+
   // WindowTreeHost:
   ui::EventSource* GetEventSource() override;
   gfx::AcceleratedWidget GetAcceleratedWidget() override;
Index: iridium-browser/ui/views/widget/desktop_aura/desktop_native_widget_aura.cc
===================================================================
--- iridium-browser.orig/ui/views/widget/desktop_aura/desktop_native_widget_aura.cc	2015-02-02 15:02:32.000000000 +0100
+++ iridium-browser/ui/views/widget/desktop_aura/desktop_native_widget_aura.cc	2015-02-02 15:03:03.000000000 +0100
@@ -593,6 +593,11 @@
   drop_helper_->ResetTargetViewIfEquals(view);
 }
 
+void DesktopNativeWidgetAura::SetSessionID(unsigned int sessionid) {
+  desktop_window_tree_host_->SetSessionID(sessionid);
+  VLOG(1) << "session id Sent to treehost";
+}
+
 void DesktopNativeWidgetAura::SetNativeWindowProperty(const char* name,
                                                       void* value) {
   if (content_window_)
Index: iridium-browser/ui/views/widget/desktop_aura/desktop_native_widget_aura.h
===================================================================
--- iridium-browser.orig/ui/views/widget/desktop_aura/desktop_native_widget_aura.h	2015-02-02 15:02:32.000000000 +0100
+++ iridium-browser/ui/views/widget/desktop_aura/desktop_native_widget_aura.h	2015-02-02 15:03:03.000000000 +0100
@@ -93,6 +93,8 @@
   // we are being activated/deactivated.
   void HandleActivationChanged(bool active);
 
+  void SetSessionID(uint32 sessionid);
+
  protected:
   // Overridden from internal::NativeWidgetPrivate:
   void InitNativeWidget(const Widget::InitParams& params) override;
Index: iridium-browser/ui/views/widget/desktop_aura/desktop_window_tree_host.h
===================================================================
--- iridium-browser.orig/ui/views/widget/desktop_aura/desktop_window_tree_host.h	2015-02-02 15:02:32.000000000 +0100
+++ iridium-browser/ui/views/widget/desktop_aura/desktop_window_tree_host.h	2015-02-02 15:03:03.000000000 +0100
@@ -80,6 +80,8 @@
 
   virtual bool IsVisible() const = 0;
 
+  virtual void SetSessionID(uint32 sessionid) = 0;
+
   virtual void SetSize(const gfx::Size& size) = 0;
   virtual void StackAtTop() = 0;
   virtual void CenterWindow(const gfx::Size& size) = 0;
Index: iridium-browser/ui/views/widget/desktop_aura/desktop_window_tree_host_x11.cc
===================================================================
--- iridium-browser.orig/ui/views/widget/desktop_aura/desktop_window_tree_host_x11.cc	2015-02-02 15:02:32.000000000 +0100
+++ iridium-browser/ui/views/widget/desktop_aura/desktop_window_tree_host_x11.cc	2015-02-02 15:03:03.000000000 +0100
@@ -10,6 +10,8 @@
 #include <X11/Xregion.h>
 #include <X11/Xutil.h>
 
+#include <signal.h>
+
 #include "base/basictypes.h"
 #include "base/command_line.h"
 #include "base/debug/trace_event.h"
@@ -102,6 +104,7 @@
   "_NET_WM_WINDOW_TYPE_NORMAL",
   "_NET_WM_WINDOW_TYPE_NOTIFICATION",
   "_NET_WM_WINDOW_TYPE_TOOLTIP",
+  "CHROMIUM_LINUX_SESSION_ID_PROPERTY",
   "XdndActionAsk",
   "XdndActionCopy"
   "XdndActionLink",
@@ -1337,6 +1340,22 @@
   XSetWMNormalHints(xdisplay_, xwindow_, &hints);
 }
 
+void DesktopWindowTreeHostX11::SetSessionID(uint32 session_id) {
+  if (session_id != 0) {
+    XChangeProperty(xdisplay_,
+                    xwindow_,
+                    atom_cache_.GetAtom("CHROMIUM_LINUX_SESSION_ID_PROPERTY"),
+                    XA_CARDINAL,
+                    32,
+                    PropModeReplace,
+                    reinterpret_cast<unsigned char*>(&session_id),
+                    1);
+    VLOG(1) << "Sent session id " << session_id << " to X window property";
+  } else {
+    VLOG(1) << "Session id is unlikely number.";
+  }
+}
+
 void DesktopWindowTreeHostX11::UpdateWMUserTime(
     const ui::PlatformEvent& event) {
   if (!IsActive())
Index: iridium-browser/ui/views/widget/desktop_aura/desktop_window_tree_host_x11.h
===================================================================
--- iridium-browser.orig/ui/views/widget/desktop_aura/desktop_window_tree_host_x11.h	2015-02-02 15:02:32.000000000 +0100
+++ iridium-browser/ui/views/widget/desktop_aura/desktop_window_tree_host_x11.h	2015-02-02 15:03:03.000000000 +0100
@@ -85,6 +85,8 @@
   // Deallocates the internal list of open windows.
   static void CleanUpWindowList();
 
+  void SetSessionID(uint32 sessionid);
+
  protected:
   // Overridden from DesktopWindowTreeHost:
   void Init(aura::Window* content_window,
Index: iridium-browser/ui/views/widget/native_widget.h
===================================================================
--- iridium-browser.orig/ui/views/widget/native_widget.h	2015-02-02 15:02:32.000000000 +0100
+++ iridium-browser/ui/views/widget/native_widget.h	2015-02-02 15:03:03.000000000 +0100
@@ -26,6 +26,8 @@
  public:
   virtual ~NativeWidget() {}
 
+  virtual void SetSessionID(uint32 sessionid) = 0;
+
  private:
   friend class Widget;
 
Index: iridium-browser/ui/views/widget/native_widget_aura.cc
===================================================================
--- iridium-browser.orig/ui/views/widget/native_widget_aura.cc	2015-02-02 15:02:32.000000000 +0100
+++ iridium-browser/ui/views/widget/native_widget_aura.cc	2015-02-02 15:03:03.000000000 +0100
@@ -60,6 +60,8 @@
 #include "ui/views/widget/desktop_aura/desktop_window_tree_host.h"
 #endif
 
+#include <signal.h>
+
 namespace views {
 
 namespace {
@@ -339,6 +341,11 @@
   window_->SetBounds(window_bounds);
 }
 
+void NativeWidgetAura::SetSessionID(uint32 sessionid) {
+  VLOG(1) << "I have no idea.";
+  raise(SIGABRT);
+}
+
 void NativeWidgetAura::GetWindowPlacement(
     gfx::Rect* bounds,
     ui::WindowShowState* show_state) const {
Index: iridium-browser/ui/views/widget/native_widget_aura.h
===================================================================
--- iridium-browser.orig/ui/views/widget/native_widget_aura.h	2015-02-02 15:02:32.000000000 +0100
+++ iridium-browser/ui/views/widget/native_widget_aura.h	2015-02-02 15:03:03.000000000 +0100
@@ -50,6 +50,8 @@
       internal::NativeWidgetPrivate* native_widget,
       aura::Window* window);
 
+  virtual void SetSessionID(uint32 sessionid) OVERRIDE;
+
   // Overridden from internal::NativeWidgetPrivate:
   void InitNativeWidget(const Widget::InitParams& params) override;
   NonClientFrameView* CreateNonClientFrameView() override;
